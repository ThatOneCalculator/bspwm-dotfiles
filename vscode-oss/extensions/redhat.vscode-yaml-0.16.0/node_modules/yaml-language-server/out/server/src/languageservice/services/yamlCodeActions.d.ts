import { TextDocument } from 'vscode-languageserver-textdocument';
import { ClientCapabilities, CodeAction, CodeActionParams, Connection } from 'vscode-languageserver';
import { CommandExecutor } from '../../languageserver/commandExecutor';
import { LanguageSettings } from '../yamlLanguageService';
export declare class YamlCodeActions {
    private readonly clientCapabilities;
    private indentation;
    constructor(commandExecutor: CommandExecutor, connection: Connection, clientCapabilities: ClientCapabilities);
    configure(settings: LanguageSettings): void;
    getCodeAction(document: TextDocument, params: CodeActionParams): CodeAction[] | undefined;
    private getJumpToSchemaActions;
    private getTabToSpaceConverting;
}
