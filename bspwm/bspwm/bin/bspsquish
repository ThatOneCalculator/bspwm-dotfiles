#!/bin/bash

bspc config -d $(bspc query --desktops -d) top_padding 150
bspc config -d $(bspc query --desktops -d) bottom_padding 150
bspc config -d $(bspc query --desktops -d) right_padding 150
bspc config -d $(bspc query --desktops -d) left_padding 150
